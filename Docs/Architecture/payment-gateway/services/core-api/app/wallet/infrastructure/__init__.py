"""wallet infrastructure layer — adapters between domain and the outside world.

Concrete implementations of the ports defined in
app.wallet.application.ports (added in a later phase). Repositories
live here; database-driver-specific code is contained to this layer.
"""
